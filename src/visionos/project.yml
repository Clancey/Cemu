name: CemuVisionOS
options:
  bundleIdPrefix: org.cemu
  deploymentTarget:
    visionOS: "2.0"
  xcodeVersion: "16.0"
  createIntermediateGroups: true
  generateEmptyDirectories: false

settings:
  base:
    SWIFT_VERSION: "5.9"
    CLANG_CXX_LANGUAGE_STANDARD: c++20
    CLANG_ENABLE_MODULES: YES
    ENABLE_STRICT_OBJC_MSGSEND: YES

targets:
  CemuVisionOS:
    type: application
    platform: visionOS
    deploymentTarget: "2.0"

    sources:
      - path: CemuVisionOS
        type: group
        buildPhase: sources
        excludes:
          - "Info.plist"
      - path: CemuBridge
        type: group
        buildPhase: none
        # Headers only — .mm files are compiled into libCemu_release.dylib by CMake
      - path: CemuVision-Bridging-Header.h
        buildPhase: none

    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: org.cemu.CemuVisionOS
        SWIFT_OBJC_BRIDGING_HEADER: CemuVision-Bridging-Header.h
        CLANG_CXX_LANGUAGE_STANDARD: c++20
        HEADER_SEARCH_PATHS:
          - "$(inherited)"
          - "$(SRCROOT)/../../src"
          - "$(SRCROOT)/../../dependencies/metal-cpp"
          - "/opt/homebrew/include"
        LD_RUNPATH_SEARCH_PATHS:
          - "$(inherited)"
          - "@executable_path/Frameworks"
        GCC_PREPROCESSOR_DEFINITIONS:
          - "$(inherited)"
          - "TARGET_OS_VISION=1"
          - "VISIONOS=1"
          - "ENABLE_METAL=1"
        SWIFT_ACTIVE_COMPILATION_CONDITIONS:
          - "$(inherited)"
          - VISION_OS
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ENABLE_PREVIEWS: YES
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: 1
        SWIFT_EMIT_LOC_STRINGS: YES
        DEVELOPMENT_TEAM: ""

    dependencies:
      - framework: ../../build_xcframework/CemuCore.xcframework
        embed: true
      - framework: ../../build_xcframework/glslang.xcframework
        embed: true
      - framework: ../../build_xcframework/SPIRVToolsOpt.xcframework
        embed: true
      - framework: ../../build_xcframework/SPIRVTools.xcframework
        embed: true

    info:
      path: CemuVisionOS/Info.plist
      properties:
        CFBundleDevelopmentRegion: en
        CFBundleDisplayName: Cemu
        CFBundleExecutable: "$(EXECUTABLE_NAME)"
        CFBundleIdentifier: "$(PRODUCT_BUNDLE_IDENTIFIER)"
        CFBundleInfoDictionaryVersion: "6.0"
        CFBundleName: "$(PRODUCT_NAME)"
        CFBundlePackageType: APPL
        CFBundleShortVersionString: "$(MARKETING_VERSION)"
        CFBundleVersion: "$(CURRENT_PROJECT_VERSION)"
        LSRequiresIPhoneOS: true
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: true
          UISceneConfigurations: {}
        UILaunchScreen: {}
        UIApplicationSupportsIndirectInputEvents: true
